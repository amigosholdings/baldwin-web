diff --git a/trackmyhairloss-pages/ops/index.html b/trackmyhairloss-pages/ops/index.html
index 4c10d1f..a54b9e3 100644
--- a/trackmyhairloss-pages/ops/index.html
+++ b/trackmyhairloss-pages/ops/index.html
@@ -130,7 +130,7 @@ async function importCsvFile(file){
 async function load(){TOKEN=$('token').value.trim();if(!TOKEN){$('status').textContent='ADMIN_TOKEN required.';return}localStorage.setItem('baldwin_admin_token',TOKEN);$('status').textContent='Loading…';try{data=await api('/v1/admin/dashboard');render();await loadContent(true);$('status').textContent=`Loaded ${data.outreach.length} prospects, ${data.providers.length} providers, ${data.email_messages.filter(m=>m.direction==='inbound'&&!m.handled_at).length} pending replies.`}catch(e){$('status').textContent='Could not load dashboard. Check ADMIN_TOKEN and Worker deployment.'}}
 window.setStage=async(type,id,stage)=>{try{await api(`/v1/admin/${type==='lead'?'leads':'outreach'}/${id}/stage`,{method:'PATCH',body:JSON.stringify({stage})});const row=(type==='lead'?data.leads:data.outreach).find(x=>x.id===id);if(row)row.stage=stage;$('status').textContent='Stage updated.'}catch(e){$('status').textContent='Stage update failed.'}}
 window.copyText=t=>navigator.clipboard.writeText(t).then(()=>$('status').textContent='Copied.');
-function outreachCopy(o){const subject='A simpler way for patients to track hair progress';const body=`Hi ${o.practice_name} team —\n\nI built Baldwin, an iPhone app that gives hair-loss patients a consistent way to photograph and compare their progress between visits.\n\nI’m opening a small free pilot for independent hair-restoration practices. There’s no software for staff to learn: patients scan a practice-specific QR code at checkout, capture a baseline, and Baldwin handles the follow-up.\n\nWould you be open to a 10-minute look this week?\n\nBest,\nShaun\nBaldwin\nhttps://trackmyhairloss.com/providers/`;return{subject,body}}
+function outreachCopy(o){const subject='A simpler way for patients to track hair progress';const body=`Hi ${o.practice_name} team —\n\nI built Baldwin, an iPhone app that gives hair-loss patients a consistent way to photograph and compare their progress between visits.\n\nI’m opening a small free pilot for independent hair-restoration practices. There’s no software for staff to learn: patients scan a practice-specific QR code at checkout, capture a baseline, and Baldwin handles the follow-up.\n\nWould you be open to a 10-minute look this week?\n\nBest,\nSean\nBaldwin\nhttps://trackmyhairloss.com/providers/`;return{subject,body}}
 window.copyEmail=id=>{const o=data.outreach.find(x=>x.id===id);if(!o)return;const c=outreachCopy(o);navigator.clipboard.writeText(`Subject: ${c.subject}\n\n${c.body}`).then(()=>$('status').textContent=`Email copied for ${o.practice_name}.`)};
 window.sendOne=async id=>{const o=data.outreach.find(x=>x.id===id);if(!o)return;if(!confirm(`Send the Baldwin pilot email to ${o.practice_name} at ${o.email}?`))return;$('status').textContent=`Sending to ${o.practice_name}…`;try{await api(`/v1/admin/outreach/${id}/send`,{method:'POST',body:'{}'});await load();$('status').textContent=`Sent to ${o.practice_name}.`}catch(e){$('status').textContent=`Send failed: ${e.message}`}}
 async function sendBatch(limit){if(!confirm(`Send the next ${limit} unsent Priority 1 prospects? This will send real email.`))return;$('status').textContent=`Sending up to ${limit} Priority 1 emails…`;try{const r=await api('/v1/admin/outreach/send-batch',{method:'POST',body:JSON.stringify({priority:1,limit})});await load();$('status').textContent=`Batch finished: ${r.sent} email${r.sent===1?'':'s'} sent.`}catch(e){$('status').textContent=`Batch failed: ${e.message}`}}
diff --git a/worker/src/index.js b/worker/src/index.js
index 2900cd5..57ce8ec 100644
--- a/worker/src/index.js
+++ b/worker/src/index.js
@@ -149,7 +149,7 @@ function replyAddress(env, outreachId) {
 
 function outreachCopy(outreach) {
   const subject = 'A simpler way for patients to track hair progress';
-  const body = `Hi ${outreach.practice_name} team —\n\nI built Baldwin, an iPhone app that gives hair-loss patients a consistent way to photograph and compare their progress between visits.\n\nI’m opening a small free pilot for independent hair-restoration practices. There’s no software for staff to learn: patients scan a practice-specific QR code at checkout, capture a baseline, and Baldwin handles the follow-up.\n\nWould you be open to a 10-minute look this week?\n\nBest,\nShaun\nBaldwin\nhttps://trackmyhairloss.com/providers/`;
+  const body = `Hi ${outreach.practice_name} team —\n\nI built Baldwin, an iPhone app that gives hair-loss patients a consistent way to photograph and compare their progress between visits.\n\nI’m opening a small free pilot for independent hair-restoration practices. There’s no software for staff to learn: patients scan a practice-specific QR code at checkout, capture a baseline, and Baldwin handles the follow-up.\n\nWould you be open to a 10-minute look this week?\n\nBest,\nSean\nBaldwin\nhttps://trackmyhairloss.com/providers/`;
   return { subject, body };
 }
 
@@ -332,7 +332,7 @@ There is no clinic login or patient upload workflow. Patients scan your link, ta
 If you would prefer physical cards for the front desk, reply with the best mailing address and I’ll send a small starter stack at no cost.
 
 Best,
-Shaun
+Sean
 Baldwin
 https://trybaldwin.app/`;
   return { subject, body, referralUrl, kitUrl };
